cmake_minimum_required(VERSION 2.6)

set(PACKAGENAME @PROJECTNAME@)
message(STATUS "Found ${PACKAGENAME}: Using ${PACKAGENAME}-config.cmake")

get_filename_component(${PACKAGENAME}_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(${PACKAGENAME}_TARGET "")
set(${PACKAGENAME}_LIBRARIES "")
set(${PACKAGENAME}_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")
